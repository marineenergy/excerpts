---
title: "excerpts"
params:
  id: 1
---


```{r setup}
# quarto render _ferc.qmd -P id:2 -o ferc_2.html
librarian::shelf(
  glue, here)
dir_repos   <- "/share/github"
dir_scripts <- glue("{dir_repos}/apps_dev/scripts")
source(glue("{dir_scripts}/common.R"))
source(glue("{dir_scripts}/db.R"))

# params <- list(
#   id = 2)
# dbListTables(con)


x <- tbl(con, "ferc_docs") |> 
  filter(
    rowid == !!params$id) |> 
  collect()
```

- Excerpt: \
  `r x$detail`

- Project: `r x$project`

- Document: `r x$prj_document`



